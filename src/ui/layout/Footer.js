import React from 'react'
import { Layout } from 'antd'

const Footer = (props) => {

    const footerStyles = {
        // zIndex: 1,
        // position: "absolute",
        // padding:5,
        // marginTop:20,
        height: "50px",
        // marginTop:"-50px"
        // bottom:0
    }

    return (
        <div style={{ footerStyles }}>
            <div>Footer</div>
        </div>

    )
}

export default Footer